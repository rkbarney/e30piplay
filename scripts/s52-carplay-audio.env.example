# Copy to ~/.config/s52-carplay-audio.env on the Pi (same user as labwc / kiosk).
# Labwc autostart sources this file before starting react-carplay so Electron
# picks the right output (USB DAC) instead of HDMI or a silent default.
#
# List sinks (PipeWire on Bookworm):
#   wpctl status
#   pactl list sinks short
#
# Set system default (affects all apps; often enough by itself):
#   wpctl set-default <id>
#
# Or force only CarPlay (Electron) via Pulse compat names:
#export PULSE_SINK=alsa_output.usb-YourVendor_YourProduct-00.analog-stereo
#
# If audio still goes nowhere, check mixer levels for the USB card:
#   alsamixer -c <card index from aplay -l>
#export ALSA_CARD=1
