# Dev preview: nginx + built app on http://localhost:8080
#   docker compose up --build

services:
  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    ports:
      - "8080:80"
